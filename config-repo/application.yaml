spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ./config-repo

server:
  port: 8090

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}